<section class="row color-group red-bricks">
  <!-- <button type="button" >{{link-to  'Cart' 'cart'}}</button> -->
  <h2>Red</h2>
  {{#each-in model as |size stats|}}
    <div class="col-md-4 well">
      <p>Size: {{size}}</p>
      <p>Price: {{stats.price}}</p>
      <p>Quantity: {{stats.quantity}}</p>
      <button type="button" {{action "addToCart"  size stats.price }}>Add To Cart</button>
    </div>
  {{/each-in}}
</section>

{{outlet}}
